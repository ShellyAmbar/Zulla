import React, { PureComponent } from "react";
import "./sideBar.css";

class SideBar extends PureComponent {
  constructor(props) {
    super(props);

    this.state = {
       
    };
  }

  render() {
    return (
      <div>
       
      </div>
    );
  }
}

export default SideBar;
